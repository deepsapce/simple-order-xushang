# 监听所有网络接口，容器内部端口 10911
listenPort=10911

# 这里设置为宿主机可达的 IP 或 host.docker.internal，如果应用在宿主机用 127.0.0.1 就能连
# 注意：对于 Mac/Windows Docker Desktop，可以使用 host.docker.internal
# 我们设为 127.0.0.1，因为应用也跑在宿主机上
brokerIP1=127.0.0.1

# 如果需要支持多网卡，可以加 brokerIP2，这里不需要